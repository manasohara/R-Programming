salary<-c(10000,20000,30000)
names(salary)<-c("Durvankur","Manas","Harshi")
salary
#it will add names to salary vector
salary['Manas'] # to access salary of manas
names(salary)<-NULL #it'll remove names
salary
